# Custom Providers

Bone does not load provider implementations from third-party extensions. Configure supported provider endpoints, headers, and models through Bone's built-in provider and model settings instead. See [Providers](providers.md) and [Custom Models](models.md).

Pi extension factories and `ExtensionAPI` provider registration are unsupported and are not part of Bone's public SDK.
