# TUI Components

Bone uses `@frelion/bone-tui` internally for its terminal interface. Bone does not expose extension UI components as a third-party customization API.

Use Bone's local skills, prompt templates, themes, and built-in settings for supported customization.
